import from "./terrain" private terrain

class CorpusGeometryManifest {
    list<terrain.TerrainMesh> objects
}

fn corpus_scene_geometry() → CorpusGeometryManifest {
    return CorpusGeometryManifest {objects = terrain.terrain_meshes()}
}
