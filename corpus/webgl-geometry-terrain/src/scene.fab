importa ex "./terrain" privata terrain

genus CorpusGeometryManifest {
    lista<terrain.TerrainMesh> objects
}

functio corpus_scene_geometry() → CorpusGeometryManifest {
    redde CorpusGeometryManifest { objects = terrain.terrain_meshes() }
}
